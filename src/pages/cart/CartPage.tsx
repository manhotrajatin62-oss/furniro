import Hero from "../../components/Hero"
import CartTotal from "./CartTotal"

const CartPage = () => {
  return (
    <section>

        {/* hero section */}
        <Hero/>

        {/* cart total section */}
        <CartTotal/>
    </section>
  )
}

export default CartPage